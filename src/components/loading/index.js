import React from 'react'

const style = {
    fontSize: '1.5rem',
    padding: '0 1rem'
}

const LoadingComponent = () => (
    <div style={style}>
        <p>Loading ...</p>
    </div>
)

export default LoadingComponent